# ============================================
# E-COMMERCE ADMIN PANEL - ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and fill in the values

# ============================================
# GITHUB API (for data storage)
# ============================================
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo (full control)
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# ============================================
# NEXTAUTH CONFIGURATION
# ============================================

# NextAuth Secret - REQUIRED
# Generate with: openssl rand -base64 32
# This MUST be changed to a unique random string in production!
NEXTAUTH_SECRET=your-super-secret-key-change-in-production-min-32-chars

# NextAuth URL - Your application's base URL
# For local development:
NEXTAUTH_URL=http://localhost:3000
# For production, use your actual domain:
# NEXTAUTH_URL=https://your-admin-panel.com

# ============================================
# GOOGLE OAUTH CREDENTIALS
# ============================================
# Get these from: https://console.cloud.google.com/apis/credentials
#
# Setup steps:
# 1. Go to Google Cloud Console
# 2. Create a new project (or select existing)
# 3. Enable "Google+ API" or "Google Identity API"
# 4. Go to "Credentials" > "Create Credentials" > "OAuth client ID"
# 5. Application type: "Web application"
# 6. Add authorized JavaScript origins:
#    - http://localhost:3000 (for development)
#    - https://your-domain.com (for production)
# 7. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google (for development)
#    - https://your-domain.com/api/auth/callback/google (for production)
# 8. Copy the Client ID and Client Secret below

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ============================================
# ADMIN ACCESS CONTROL (STRICT ALLOWLIST)
# ============================================
# IMPORTANT: Only these email addresses can log in to the admin panel!
# All other Google accounts will be DENIED access.
#
# Format: Comma-separated list of Gmail addresses
# Example: admin1@gmail.com,admin2@gmail.com,manager@company.com
#
# SECURITY NOTES:
# - If this list is empty, ALL access is denied for safety
# - Users can be removed by simply removing their email from this list
# - Changes take effect immediately (no restart required)
# - Keep this list as small as possible (principle of least privilege)

ALLOWED_ADMIN_EMAILS=your-email@gmail.com
